# 2026-09-09 工作日志（晚间复核会话）

## 任务
panda 要求：① 仔细通读项目内容；② 核对项目进度与文档一致性；③ 记住开发过程中的要求；④ 核对 `docs/audit-report.md` 指出的问题是否已解决。

## 通读范围与结论
- 通读：docs/plan.md（815 行全文，权威进度源）、project.md、developlan.md、audit-report.md、final-review.md、redis-summary 要点、storage.md 要点、CLAUDE.md、.workbuddy/memory（既有 3 文件）；抽查 interface.md/database.md 关键契约段；源码核验以后端 88 个 Java 文件 + 前端 16 个源文件为主。
- **进度一致性：✅ 一致**。plan.md 路线图（4 阶段全部 ✅，收尾 c1f689e + Day 6 7e3d92c + 底稿 97e5bd4）与 git 提交链、代码结构、接口清单（六 接口状态表逐条对上 Controller）、Redis 键表、5 张表 schema 均吻合；**92 用例** 与 `target/surefire-reports`（09-09 17:20，24 类 / 92 run / 0 Failures / 0 Errors）吻合。
- 细节差异（不影响结论）：
  1. plan.md L40 写「HEAD c035ca8」→ 实际 97e5bd4（后 3 个文档提交未推 origin，领先 3）；
  2. 工作区 135 文件 unstaged「修改」= CRLF 换行噪音（`--ignore-cr-at-eol` diff 为空，内容零差异）；提交前需处理行尾，勿整体提交；
  3. 本 Linux 环境无 JDK（`java: command not found`），mvn 全量回归须回开发机执行（先停 dev 后端防 auto-poll 抢消费，plan.md 回归教训④）。

## audit-report.md（R/C/E 版）逐项核验结果（静态核验于 HEAD 97e5bd4 代码）
| 项 | 结论 | 证据（当前代码） |
|---|---|---|
| R1 ID 超长失真 | ✅ 已解决 | IdGenerator 重构为 <2^53（JS 安全整数，EPOCH 2024 左移 10 位），前端全程字符串透传去 Number() 回环 |
| R2 发消息失败不回滚 | ✅ 已解决 | SeckillServiceImpl 捕获 XADD 异常 → 反向补偿（INCR 回补库存 + DEL 令牌）→ 重抛，注释写明语义 |
| R3 无鉴权+密钥明文 | ✅ 已解决 | ApiAccessInterceptor/SecurityProperties/UserTokenRegistry；yaml 全 `${ENV:}` 绑定；40903 动态注册 |
| R4 更新不清预热缓存 | ✅ 已解决 | 更新成功 evictActivityCache + preheat_status 置 0；@Version 乐观锁，0 行更新报 PARAM_ERROR |
| R5 毒消息留 PEL/死信无回放 | ✅ 已解决 | 宽松解析 lenient 分支 → 统一 handleFailure 计数 → 超阈值转死信；DeadLetterReplayService + POST /api/admin/seckill/dead-letters/replay |
| C1/C2 订单状态折叠/字段名 | ✅ 已解决 | OrderController 按 OrderStatusEnum 输出 status/statusDesc + createdAt |
| C3 users orders 接口 | ✅ 已解决 | GET /api/seckill/users/{userId}/orders?activityId=（可选归属校验 M9） |
| C4 全局异常缺项 | ✅ 已解决 | 补 MissingServletRequestParameter/MissingPathVariable/TypeMismatch/MaxUpload/NoResourceFound(404) |
| C5 check/execute 口径 | ✅ 已解决 | CANCELLED/未开始/已结束/仅时间窗内未预热 → 细分 reason，不放行，与 execute 同源 |
| C6 失败响应 data=null | ✅ 已解决 | SeckillExecuteController 按 ResultCode 细分 SOLD_OUT/DUPLICATED/RATE_LIMITED/REJECTED（M1） |
| E1 resetStock 死代码 | ✅ 已闭环 | decrStockScript Bean 恢复（回归教训①）；E1-r 管理端 reset-stock + 进行中守卫（AdminSeckillResetStockTest +4） |
| E2 limitPerUser 无效 | ✅ 已解决 | @Min/@Max 夹逼=1 + Service 兜底 + 前端 max=1 提示 |
| E3 创建缺业务校验 | ✅ 已解决 | 价格 ≥0、库存 ≥1、endTime>startTime（Service 286 行）、productId 存在性预查（293 行）；CreateBusinessValidationTest +10 |
| E4 限流粒度 | ✅ 已解决 | 键双维 rate:limit:{activityId}:{userId}（SeckillServiceImpl:100）+ flash.rate-limit.window-seconds/max-count yaml 化 |
| E5 文案 | ✅ 已解决 | 「傻鸟」全文 0 命中；上传失败固定可读文案 |
| E6 上传校验 | ✅ 核心已解决 | AbstractImageStorage：扩展名白名单 + 文件头魔数逐格式校验（PNG/JPEG/GIF/BMP/WEBP）+ 服务端按扩展名固定 Content-Type + 5MB；⚠️「合法图片误拒」复现仍挂起（final-review §五.2，需失败样本） |
| S1 密钥硬编码 | ✅ 已解决 | AiLlmProperties 默认空串 + ${AI_LLM_API_KEY:}；全历史无入库 |
| S2 Redis 加固 | ✅ 已解决 | BasicPolymorphicTypeValidator 白名单 + ${REDIS_PASSWORD:} |
| M1~M10 | ✅ 已解决 | M3 requestId 走 RequestIdUtil(yyyyMMddHHmmss+4位)；M4 execute 缓存未命中按实时时间窗细分（先 DB 时间窗 assertOpenWindow 再报未预热）；M7 AiChatServiceImpl 目录按 endTime 过滤 + 实时状态文案；M8 ActivityItemVO.preheated；M10 详情页「我的抢购记录」面板 |

## 前端 F 系列（audit 第四节；plan/final-review 未逐条登记，属跟踪缺口）
- ✅ 已解决：F1（won/duplicated/lost 后按钮置灰，btnDisabled 绑定）、F4（轮询 catch 有 toastWarn + 超时 toastErr）、F8（document.hidden 统一暂停：ListView/DetailView/RankBoard/MetricsPanel）。
- ⏳ 未解决：F2（AiServiceView 侧栏仍按后端 status 快照 filter/tag，活动 DB status 不自动翻转 RUNNING → 恒判「即将开抢」；该文件审计后再无提交）、F3（ActivityListView.load catch 仍静默置空 activities=[]，无 toastErr）、F5（AdminView.onFileChange 无前端 MIME/≤5MB 预检，靠服务端兜底）、F6（AiServiceView.sendText 无 typing 防重入入口，按钮仅按输入为空禁用，回车可连发）。
- ◐ 部分：F7（「演示用户 #1001」硬编码已随账号体系消除；改 productId 后保存仍无 watch 重载/比对，靠「← 切换商品/载入商品」手动流程）。
- 低残留：F9（App.vue menu-toggle 无 handler；AiServiceView 🎤/📎 死按钮；format.js STATUS_TEXT 未用导出 = final-review O10）。

## 记住的要求（开发约定，同时存于 MEMORY.md/plan.md「必读」）
1. 称呼 panda；始终简体中文；导师引导+检查为主；2026-09-09 起功能由 AI 实现、panda 终审；阻塞性环境问题导师介入。
2. 每个功能模块完成必须全量 `mvn test` BUILD SUCCESS；收尾全量跑，停 dev 后端后再跑。
3. 经验教训记入 plan.md；教学式注释容忍不删；plan.md 进度由导师维护。
4. 技术红线：金额 Long 分；UTC 存储/Asia-Shanghai 展示；ID 应用生成（order 表主键 AUTO）；Redis db1（redis-cli -n 1）；缓存 TTL 300+random(60)s、空值 "" 60s、三态读取；键收敛 CacheKeyConstant；redis-cli keys 带 * 且 !isEmpty() 才删；R3 令牌强绑定、S2 白名单不可回退；种子活动时间需在未来。
5. 演示要求：多用户账号可配置（.env USER_TOKENS ↔ 前端 DEMO_ACCOUNTS 两处一致）；自定义 userId 可经 /api/auth/register 动态领令牌抢购；前端 ID 字符串透传防精度丢失。

## F2/F3/F5/F6 修复实现记录（本日晚间，AI 实现 · 待 panda 终审）

按 panda 指示修复 audit 前端红线遗留项（纯前端 3 文件，无后端改动）：

- **F2**（AiServiceView.vue）：新增 `livePhaseOf(a, now)` 本地实时推导（CANCELLED→排除；now<start→预告；now≥end→已结束；stock=0→已售罄；否则秒杀中），热门侧栏只推荐「秒杀中/即将开抢」、进行中优先；导入 parseServerTime/onBeforeUnmount；30s 静默刷新且 document.hidden 暂停。
- **F3**（ActivityListView.vue）：load() catch 内 toastErr + 连续失败单次提醒标记（8s 轮询防刷屏）。
- **F5**（AdminView.vue）：onFileChange 前端预检 MIME∪扩展名白名单（jpeg/png/webp/gif/bmp）+ ≤5MB，与后端 AbstractImageStorage 对齐。
- **F6**（AiServiceView.vue）：sendText 入口 `if (typing.value) return` + 发送按钮 :disabled 追加 typing。

验证：Windows 装的 node_modules 在 Linux 侧缺 rollup/esbuild 原生二进制，`npm ci`（本地缓存 .npm-cache-local，用完即删）后 `node node_modules/vite/bin/vite.js build` ✅ 1.41s。真实内容 diff 仅 3 个 vue 文件（+61/-10）；plan.md 已记当日卡；F7 半/F9 低/E6 复现仍挂起。
