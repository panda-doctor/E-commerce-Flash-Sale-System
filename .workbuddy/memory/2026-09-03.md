# 2026-09-03 工作日志

## 第 2 阶段 Day 1 验收通过（Lua 库存扣减）

panda 修复 `LuaStockDeductionTest` 后，两轮审查-修改闭环完成：

### 审查发现的问题（panda 修改项）
1. 场景 4 并发逻辑错误（初版 10 线程各扣 1 次 = 共 10 次请求，50 库存扣不完，断言期望 0 实际 40）→ panda 改为每线程 `while(true)` 循环抢到 -1 才退出。
2. `setUp` 清键死代码：`keys(STOCK_KEY_PREFIX + "")` 缺 `*` 通配符 + `keys.isEmpty()` 判空写反（Day 7 教训 2 同款）→ panda 修正为 `"*"` + `!isEmpty()`。

### 验证结果
- 单类 4 场景全绿；并发场景实测：**成功扣减 50、失败拦截 10、库存归 0**，无超卖实证成立。
- 全量 `mvn test`：**Tests run: 19, Failures: 0, Errors: 0 → BUILD SUCCESS**。

### 进度文档（导师已更新）
- `docs/plan.md`：Day 1 任务 1.1~1.3 → ✅，验收标准 ✅ 达成，写 Day 1 经验教训 4 条（并发测试请求量要大于库存、keys 通配符、setUp 兜底、本机 Maven 损坏绕行方案），脚注更新，下次开始位置=Day 2 execute 接口。

### 环境问题（本机 Maven 损坏，导师介入）
- 用户级 `MAVEN_HOME=D:\Maven\apache-maven-3.9.4` 损坏，`mvn`/`mvnw` 报 `classworlds Launcher CNFE`。
- 绕行：用 wrapper 缓存 Maven 3.9.16（`~/.m2/wrapper/dists/apache-maven-3.9.16/<hash>/`），手动 `java -classpath boot jar + -Dclassworlds.conf + Launcher` 启动；路径须 `C:/...`（`/c/...` 传 Windows 程序会转成 `\c\...` 报 NoSuchFile）。
- 该命令模板已写入 plan.md Day 1 经验教训 4。

### 遗留小项
- `RedisConfig` 第 9 行未使用 import `RedisScriptingCommands`（仅 IDE 警告，不阻塞）——待 panda 顺手删。
- 全量通过后可 git 提交（建议 commit 信息「完成第 2 阶段 Day 1：库存扣减 Lua 脚本与测试」）。

## Day 1 git 提交完成
- commit `8d2d7cf`「完成第 2 阶段 Day 1：库存扣减 Lua 脚本与并发防超卖测试」，6 files（+266/-512）。
- panda 已删 RedisConfig 多余 import；RedisConfig.java、LuaStockDeductionTest.java、decr_stock.lua、docs/plan.md 等一并入库；docs/day.md 与 lua/.gitkeep 删除一并提交；`.workbuddy/` 未跟踪不提交。
- 途中排障：git index.lock 过期锁（无活动进程，rm 后恢复）；git add 已删文件 pathspec 报错（删除已在暂存区无需再 add）。

## Day 2 任务布置（2026-09-03 13:31）
- panda 开工 `/api/seckill/execute`。新类：`SeckillRequest` DTO（activityId/userId @NotNull，productId 可选）→ `SeckillService` + Impl（execute：活动存在→时间窗→状态→Lua 扣减→QUEUED；-1 抛 40902）→ `SeckillExecuteController`（POST /api/seckill/execute，@Valid @RequestBody）→ `SeckillExecuteResponse`（result/message/orderNo 预留）。
- 本轮范围边界：**不写订单、不做用户防重（Day 4）、不做 Redis Stream（后续阶段）**；Lua 扣减走 `redisTemplate.execute(decrStockScript, ...)`。
- 测试验收口径（对应 plan.md 2.4）：进行中扣到 0 / 已结束 40001 / 售罄 40902 / 参数缺失 40001；沿用真实 Redis + @MockBean Mapper、IdGenerator 活动ID、setUp 清键、`!keys.isEmpty()` 教训。
- 种子活动 endTime 易过期，测试活动须动态构造 start/end（now±1h）；DB status 可能滞后，时间窗判活动开放为主、显式 SOLD_OUT/ENDED 状态为辅。
