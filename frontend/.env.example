# =====================================================
# 闪电秒杀 · 前端环境变量模板
# =====================================================
# 用法：复制本文件为同目录 .env 并填写；.env 已被 git 忽略。
# 加载：Vite 自动读取 frontend/.env（npm run dev / build 均生效），
#       仅注入 VITE_ 前缀变量到 import.meta.env。
#
# 注意：值必须与后端 E-commerceFlashSaleSystem/.env 一一对应，
#       不一致会导致对应接口报"令牌无效"。

# 管理端令牌 → 请求头 X-Admin-Token（管理控制台 /api/admin/** 使用）
# 需与后端 ADMIN_TOKEN 一致
VITE_ADMIN_TOKEN=admin-secret-2026

# 用户访问令牌 → 请求头 X-User-Token（秒杀大厅 execute / 活动校验 / 查订单使用）
# http.js 优先按当前 userId 从前端 store.js DEMO_ACCOUNTS 取匹配令牌；此处是「兜底回落令牌」，
# 仅当切换到未注册账号的自定义 userId 时使用，仍须是后端 USER_TOKENS 列表中的一条令牌
VITE_USER_TOKEN=user-a
